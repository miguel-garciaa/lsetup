ssh-keygen -t ed25519 -C "almalinux"

type $env:USERPROFILE\.ssh\id_ed25519.pub | ssh miguel@192.168.1.10 "mkdir -p ~/.ssh && chmod 700 ~/.ssh && cat >> ~/.ssh/authorized_keys && chmod 600 ~/.ssh/authorized_keys"